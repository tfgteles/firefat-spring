package com.firefat.backend.repo;

import com.firefat.backend.domain.HeightUnit;
import org.springframework.data.jpa.repository.JpaRepository;

public interface HeightUnitRepo extends JpaRepository<HeightUnit, Long> {
}
